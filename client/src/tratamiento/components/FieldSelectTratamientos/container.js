import { connect } from 'react-redux'


import FieldSelectTratamientos from './FieldSelectTratamientos'

function mapStateToProps(state) {
	return {
		
	}
}

function mapDispatchToProps(dispatch) {
	return {
	}
}

export default connect(mapStateToProps, mapDispatchToProps)(FieldSelectTratamientos)