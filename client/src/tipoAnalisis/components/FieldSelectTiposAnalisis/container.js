import { connect } from 'react-redux'

import FieldSelectTiposAnalisis from './FieldSelectTiposAnalisis'

function mapStateToProps(state) {
	return {
		
	}
}

function mapDispatchToProps(dispatch) {
	return {
	}
}

export default connect(mapStateToProps, mapDispatchToProps)(FieldSelectTiposAnalisis)