import { connect } from 'react-redux'

import Autenticar from 'Autenticar'

function mapStateToProps(props) {
	return {

	}
}

function mapDispatchToProps(dispatch) {
	return {

	}
}

export default connect(mapStateToProps, mapDispatchToProps)(Autenticar)